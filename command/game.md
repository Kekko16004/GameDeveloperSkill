---
description: Idea to playable slice. One macrotask per chat then HARD STOP. Parent only.
---

Call Skill tool `game-developer` FIRST. Never Read `references/*.md` from the Unity cwd.

You are PARENT only. For: $ARGUMENTS

1. Follow workers.md + pipeline.md + session-cuts.md from the skill payload.
2. Se session-mode e hard-stop: dopo GDD lock fai ONE macrotask (greybox OR kit-fetch OR one kit-dress OR one blender prop OR one UI screen), poi HARD STOP banner. Se continuous (con subagents): prosegui delegando ogni macrotask a un subagent pulito senza fermare la chat.
3. Do not implement yourself. Task general, 8 context lines, SKILL=absolute skill dir.
4. Art default = Kenney/KayKit/Quaternius Standard + ProBuilder. Blender = USA SEMPRE LA SUITE COMPLETA DEI TOOL NATIVI (search_polypizza, download_polypizza, get_scene_info, get_object_info, get_viewport_screenshot, export_scene). Vietato fossilizzarsi o bloccarsi su execute_blender_code (ammesso solo micro-sanitize finale). Forbidden Sloyd/Meshy/Hunyuan. Forbidden http://localhost:9876/mcp — use uvx blender-mcp stdio. GLB <15KB = FAIL.
5. UI: Skill `real-world-design` required. Hand-written UXML first = FAIL.
6. Claiming Blender without screenshots/blender-*-2-import.png = FAIL. Claiming kits without screenshots/030-import-*.png = FAIL.
7. In modalita hard-stop, se l'utente dice procedi dopo HARD STOP: rifiuta e indica /resumegame in chat nuova. In modalita continuous, procedi normalmente.

Resume = `/resumegame`.
